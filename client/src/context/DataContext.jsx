/**
 * DataContext.jsx -- Application Data Context (placeholder)
 *
 * Purpose:
 *   Reserved for a shared data-layer context that would provide cached
 *   domain data (companies, ads, campaigns, etc.) to the component tree,
 *   complementing the authentication state in AuthContext.
 *
 * Status: not yet implemented.
 *
 * Connections:
 *   - Would typically be mounted alongside AuthProvider in main.jsx.
 *   - Consumer components would use a useData() hook similar to useAuth().
 */
